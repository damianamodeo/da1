/* eslint-disable-next-line */
export type ShellTestingProps =  {}

export const ShellTesting = (props: ShellTestingProps) => {
  return (
    <div>
      <h1>Welcome to ShellTesting!</h1>
    </div>
  );
};

export default ShellTesting;
